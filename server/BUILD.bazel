load("@aspect_rules_ts//ts:defs.bzl", "ts_config")

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [
        "//:tsconfig",
        "//common:tsconfig",
    ],
    visibility = [
        "//server:__subpackages__",
        "//integration:__subpackages__",
    ]
)
